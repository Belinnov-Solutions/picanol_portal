@using (Html.BeginForm("UploadChallan", "Home", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    <div class="col-lg-3">
        <input type="file" name="UploadedFile" />
    </div>
    <div class="col-lg-2">
        <input type="submit" name="Submit" id="upload" value="Submit" />
        <br/>
    </div>
}


@using (Html.BeginForm("UploadOrder", "Home", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    <div class="col-lg-3">
        <input type="file" name="UploadedFile" />
    </div>
    <div class="col-lg-2">
        <input type="submit" name="Submit" id="upload" value="Submit" />
    </div>
}
